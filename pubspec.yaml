name: ugd_modul_2_kel1
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  sqflite: ^2.3.0
  flutter_slidable: ^0.6.0
  shared_preferences: ^2.0.7
  # cunning_document_scanner: ^1.0.2
  permission_handler: ^10.4.5
  edge_detection: ^1.0.6
  path_provider: ^2.1.1
  path: ^1.8.3
  geolocator: ^10.1.0
  geocoding: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - image/
    - image/icon/
